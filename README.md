# Scheduler

Scheduler is a full-stack web application to manage and schedule your tasks and events using a dynamic calendar interface. It features user authentication, responsive UI, and real-time scheduling.

## Features

- User registration and login with secure JWT authentication
- Password hashing with bcrypt for security
- Interactive calendar to create, edit, and view tasks/events
- Responsive design using Material UI and Emotion styling
- Form validation using Formik and Yup
- REST API backend built with Express and MongoDB
- CORS enabled for cross-origin requests

## Technology Stack

### Frontend

- React
- React Router DOM
- Material UI (MUI) and Emotion for styling
- React Calendar
- Formik and Yup for forms and validation
- Axios for API calls

### Backend

- Node.js and Express.js
- MongoDB and Mongoose
- JSON Web Tokens (JWT) for authentication
- bcrypt for password hashing
- dotenv for environment variables
- CORS middleware

### Prerequisites

- Node.js installed on your machine (v16 or above recommended)
- MongoDB instance (local or cloud, e.g., MongoDB Atlas)


